<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kmerz.app.member">

<insert id="insertMember">
	insert into tbl_member(user_id, user_email, user_pw, user_name)
	values(seq_user_id.nextval, #{user_email}, #{user_pw}, #{user_name})
</insert>

<select id="selectAll" resultType="MemberVo">
	select * from tbl_member
</select>

<select id="selectEmail" resultType="MemberVo">
	select * from tbl_member where user_email = #{user_email}
</select>

</mapper>